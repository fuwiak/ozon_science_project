# Как запустить приложение

## Быстрый старт

### 1. Backend (FastAPI)

Откройте терминал и выполните:

```bash
# Перейдите в корневую директорию проекта
cd /Users/user/ozon_science_project

# Активируйте виртуальное окружение
source .venv/bin/activate

# Запустите сервер (используя скрипт)
./START_SERVER.sh

# ИЛИ вручную:
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен по адресу:
- **API**: http://localhost:8000
- **Swagger документация**: http://localhost:8000/docs
- **ReDoc документация**: http://localhost:8000/redoc

### 2. Frontend (Next.js)

Откройте **новый терминал** и выполните:

```bash
# Перейдите в директорию web-ui
cd /Users/user/ozon_science_project/web-ui

# Запустите dev сервер (используя скрипт)
./start-dev.sh

# ИЛИ вручную:
npm run dev
```

Frontend будет доступен по адресу:
- **Web UI**: http://localhost:3000

## Что происходит при запуске

### Backend:
1. ✅ Загружается стартовый файл (2-3 секунды)
2. ✅ Приложение готово к работе
3. 🔄 Остальные файлы загружаются в фоне (30-60 секунд)

### Frontend:
1. ✅ Запускается Next.js dev сервер
2. ✅ Подключается к backend на порту 8000
3. ✅ Отображает данные из кэша

## Проверка работы

1. Откройте браузер: http://localhost:3000
2. Вы должны увидеть дашборд с метриками
3. Проверьте API: http://localhost:8000/docs

## Возможные проблемы

### Порт 8000 занят
```bash
# Найдите процесс на порту 8000
lsof -ti:8000

# Убейте процесс
kill -9 $(lsof -ti:8000)
```

### Порт 3000 занят
Скрипт `start-dev.sh` автоматически очищает порты, но если нужно вручную:
```bash
# Убейте процессы на портах 3000 и 3001
lsof -ti:3000 | xargs kill -9
lsof -ti:3001 | xargs kill -9
```

### Backend не запускается
```bash
# Проверьте, что виртуальное окружение активировано
which python  # Должен показать путь к .venv

# Установите зависимости заново
pip install -r requirements.txt
```

### Frontend не запускается
```bash
# Установите зависимости
npm install

# Очистите кэш Next.js
rm -rf .next
npm run dev
```

## Структура запуска

```
Терминал 1 (Backend):
└─> uvicorn app.main:app --reload
    └─> http://localhost:8000

Терминал 2 (Frontend):
└─> npm run dev
    └─> http://localhost:3000
```

## Остановка

- **Backend**: Нажмите `Ctrl+C` в терминале с backend
- **Frontend**: Нажмите `Ctrl+C` в терминале с frontend

## Дополнительная информация

- Подробная документация API: см. `README.md`
- Быстрый старт: см. `QUICK_START.md`
- Оптимизация: см. `OPTIMIZATION.md`


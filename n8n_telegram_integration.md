# Интеграция с Telegram через n8n

Это руководство описывает, как настроить управление приложением Dynamic Pricing через Telegram бота с использованием n8n.

## Архитектура

```
Telegram Bot → n8n Webhook → FastAPI /api/telegram/webhook → Обработка команд → Ответ в Telegram
```

## Шаг 1: Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Сохраните полученный **API Token**

## Шаг 2: Настройка n8n Workflow

### Workflow структура:

1. **Telegram Trigger** - получает сообщения из Telegram
2. **HTTP Request** - отправляет команду в FastAPI
3. **HTTP Request** - отправляет ответ обратно в Telegram

### Детальная настройка:

#### Узел 1: Telegram Trigger
- **Тип**: Telegram Trigger
- **Настройки**:
  - Credential: Telegram Bot Token (создайте credential с токеном бота)
  - Updates: Messages

#### Узел 2: HTTP Request (к FastAPI)
- **Тип**: HTTP Request
- **Метод**: POST
- **URL**: `http://localhost:8000/api/telegram/webhook`
- **Body**: 
```json
{
  "message": {
    "text": "{{ $json.message.text }}",
    "chat": {
      "id": {{ $json.message.chat.id }}
    },
    "from": {
      "id": {{ $json.message.from.id }}
    }
  }
}
```

#### Узел 3: HTTP Request (ответ в Telegram)
- **Тип**: HTTP Request
- **Метод**: POST
- **URL**: `https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage`
- **Body**:
```json
{
  "chat_id": {{ $('Telegram Trigger').item.json.message.chat.id }},
  "text": "{{ $json.message }}"
}
```

## Шаг 3: Доступные команды

### `/stats` или `stats`
Получает статистику кэша:
- Количество товаров
- Количество загруженных файлов
- Размер кэша
- Режим данных (мок/реальные)

**Пример ответа:**
```
📊 Статистика кэша:

Всего товаров: 223,741
Файлов загружено: 25
Размер кэша: 45.2 МБ
Режим: Реальные данные
```

### `/cache_clear` или `cache_clear`
Очищает весь кэш

**Пример ответа:**
```
✅ Кэш очищен
Удалено товаров: 223,741
```

### `/cache_reload` или `cache_reload`
Перезагружает кэш из файлов

**Пример ответа:**
```
🔄 Кэш перезагружен
Товаров в кэше: 223,741
```

### `/products_count` или `products_count`
Показывает количество товаров в кэше

**Пример ответа:**
```
📦 Товаров в кэше: 223,741
```

### `/help` или `help`
Показывает список доступных команд

## Шаг 4: Тестирование

1. Запустите FastAPI сервер:
```bash
cd /Users/user/ozon_science_project
python3 -m uvicorn app.main:app --reload
```

2. Запустите n8n workflow

3. Отправьте команду в Telegram боту:
```
/stats
```

4. Вы должны получить ответ с статистикой

## Расширение функциональности

### Добавление новых команд

1. Откройте `app/routers/telegram.py`
2. Добавьте новый обработчик в функцию `handle_telegram_command`:

```python
elif cmd == "/new_command" or cmd == "new_command":
    # Ваша логика здесь
    return TelegramResponse(
        success=True,
        message="Ответ пользователю"
    )
```

3. Обновите список команд в `/help`

### Добавление авторизации

Для ограничения доступа можно добавить проверку `user_id`:

```python
ALLOWED_USERS = ["123456789", "987654321"]  # Telegram user IDs

if command.user_id not in ALLOWED_USERS:
    return TelegramResponse(
        success=False,
        message="❌ У вас нет доступа к этому боту"
    )
```

## Примеры использования

### Мониторинг кэша
Настройте n8n workflow для периодической проверки статистики и отправки уведомлений при достижении определенных порогов.

### Автоматическая очистка
Создайте команду для автоматической очистки кэша по расписанию.

### Уведомления
Настройте отправку уведомлений в Telegram при важных событиях (ошибки, завершение загрузки и т.д.)

## Troubleshooting

### Бот не отвечает
- Проверьте, что n8n workflow активен
- Убедитесь, что FastAPI сервер запущен
- Проверьте логи n8n и FastAPI

### Ошибки в n8n
- Проверьте правильность URL в HTTP Request узлах
- Убедитесь, что credentials настроены правильно
- Проверьте формат данных в Body узлов

### Ошибки в FastAPI
- Проверьте логи сервера
- Убедитесь, что все зависимости установлены
- Проверьте доступность эндпоинтов через Swagger: http://localhost:8000/docs



#!/bin/bash
# Скрипт для запуска сервера с предзагрузкой данных

echo "🚀 Запуск OZON Dynamic Pricing API..."
echo "📊 Данные будут загружены в фоновом режиме при старте"
echo ""

cd "$(dirname "$0")"
source .venv/bin/activate

uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
